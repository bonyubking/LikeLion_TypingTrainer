import React from "react";
import styles from './MainPage.module.css';

const MainPage = () => {
    return (
        <div className={styles.main}>메인 페이지 입니다.</div>
    );
};

export default MainPage;